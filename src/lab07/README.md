# Лабораторная работа №7 - Консольное приложение

## 1. Структура проекта

| Файл | Назначение |
|------|-----------|
| `main.py` | Загружает данные, запускает CLI, сохраняет при выходе |
| `cli.py` | Отвечает за взаимодействие с пользователем (меню, ввод, вывод) |
| `app.py` | Содержит бизнес-логику (добавление, удаление, поиск, сортировка) |
| `storage.py` | Преобразует объекты в JSON и обратно |
| `exceptions.py` | Определяет `ItemNotFoundError`, `DuplicateItemError`, `InvalidInputError` |

---

## 2. Описание CLI

### Пункты меню

| Пункт | Действие |
|-------|----------|
| 1 | Показать все серверы (таблица) |
| 2 | Добавить веб-сервер |
| 3 | Добавить сервер БД |
| 4 | Добавить файловый сервер |
| 5 | Найти сервер по имени |
| 6 | Найти сервер по IP |
| 7 | Показать активные серверы |
| 8 | Сортировать серверы |
| 9 | Удалить сервер |
| 0 | Выход (с автосохранением) |

### Обработка ошибок

| Ситуация | Обработка |
|----------|-----------|
| Ввод букв вместо числа | `InvalidInputError` → сообщение пользователю |
| Дубликат IP | `DuplicateItemError` → сообщение пользователю |
| Сервер не найден | `ItemNotFoundError` → сообщение пользователю |

### Сохранение и загрузка

- **При запуске:** автоматическая загрузка из `data.json`
- **При выходе:** автоматическое сохранение в `data.json`
- **Формат:** JSON

---

## 3. Демонстрация работы

### Сценарий 1: Запуск и вывод коллекции

При старте приложение загружает данные и отображает меню. Пункт 1 выводит таблицу всех серверов.

![47](img/lab07/img01.png)

---

### Сценарий 2: Добавление, выход и повторный запуск

Через меню добавляется новый сервер, затем выполняется выход. При повторном запуске данные восстанавливаются из файла.

![47](img/lab07/img02.png)

---

### Сценарий 3: Сортировка с выбором стратегии

В подменю сортировки доступны 4 стратегии: по имени (А→Я / Я→А) и по нагрузке (возрастание / убывание).

![47](img/lab07/img03.png)

---

### Сценарий 4: Фильтрация активных серверов

Пункт 7 выводит только серверы со статусом `active`.

![47](img/lab07/img04.png)

---

### Сценарий 5: ItemNotFoundError

Поиск по несуществующему имени или IP-адресу возвращает соответствующее сообщение об ошибке.

![47](img/lab07/img05.png)

---

### Сценарий 6: InvalidInputError

Ввод букв вместо цифр при выборе пункта меню приводит к перехвату `InvalidInputError`.

![47](img/lab07/img06.png)

---

### Сценарий 7: DuplicateItemError

Попытка добавить сервер с уже существующим IP-адресом вызывает `DuplicateItemError`.

![47](img/lab07/img07.png)

---

## 4. Вывод

В ходе лабораторной работы были изучены следующие концепции:

- **Разбиение на модули и слои** — CLI → App → Storage
- **Интерактивное CLI с меню** — навигация и ввод данных пользователем
- **Обработка пользовательского ввода и ошибок** — перехват и информирование
- **Собственные исключения** — `ItemNotFoundError`, `DuplicateItemError`, `InvalidInputError`
- **Сохранение и загрузка данных** — сериализация объектов в JSON
- **Типизация методов** — аннотации типов из ЛР-6
- **Подтверждение опасных операций** — запрос подтверждения перед удалением
